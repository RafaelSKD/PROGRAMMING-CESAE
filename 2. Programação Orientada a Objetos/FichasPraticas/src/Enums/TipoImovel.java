package Enums;

// The TipoImovel enum represents different types of real estate properties.
// It is used to categorize properties and influence their value calculation.

public enum TipoImovel {
    APARTAMENTO, // Apartment (typically smaller and part of a building)
    CASA,        // House (standalone residential property)
    MANSAO       // Mansion (large, luxurious property)
}
